# Domain docs

This is a single-context repository.

Before changing behavior, read:

- `CONTEXT.md` for canonical product language.
- Relevant records under `docs/adr/` for hard-to-reverse decisions.

Use glossary terms in code, tests, issues, and documentation. Surface conflicts
with an ADR instead of silently overriding it.
